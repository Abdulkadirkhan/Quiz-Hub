[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@9.15.4 --activate",
  "pnpm install --frozen-lockfile=false"
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm run start"
